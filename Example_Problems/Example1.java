package Example_Problems;

class Example1 {
    public static void main(String[] args) {
        // Instead of writing int, string, char we can use 'var' keyword in java the compailer will understand what type of data it is
        var age = 20;
        var name = "Hameed";
        var grade = 'A';

        System.out.println("Name: " + name + "\nAge: " + age + "\nGrade: " + grade);
    }
}